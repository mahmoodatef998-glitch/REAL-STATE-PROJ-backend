# =====================================================
# AL RABEI REAL ESTATE - Production Environment
# =====================================================
# IMPORTANT: This is for PRODUCTION deployment only!
# These variables MUST be set in your hosting platform
# DO NOT commit this file if you fill in actual values!
# =====================================================

# ============ Server Configuration ============
PORT=3000
NODE_ENV=production
LOG_LEVEL=warn

# ============ Frontend Configuration ============
# Your production domain
FRONTEND_URL=https://yourdomain.com
CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# ============ Security Configuration ============
# Generate using: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
# MUST be a strong random value (64+ characters recommended)
JWT_SECRET=GENERATE-A-STRONG-64-CHARACTER-RANDOM-VALUE-FOR-PRODUCTION-ONLY-DO-NOT-USE-EXAMPLE

# Rate Limiting - More strict for production
RATE_LIMIT_WINDOW_MS=600000
RATE_LIMIT_MAX_REQUESTS=50

# ============ Database Configuration ============
# PostgreSQL with SSL enabled
# Example formats:
# - Supabase: postgresql://user:password@host.supabase.co:5432/postgres?sslmode=require
# - Railway: postgresql://user:password@host.railway.app:5432/database?sslmode=require
# - Neon: postgresql://user:password@ep-*.neon.tech/database?sslmode=require
# - AWS RDS: postgresql://user:password@host.rds.amazonaws.com:5432/database?ssl=true
DATABASE_URL=postgresql://user:password@production-host:5432/al_rabei_real_estate?sslmode=require

# ============ File Upload Configuration ============
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/jpg,image/png,image/webp

# ============ Email Configuration (Optional) ============
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password
# EMAIL_FROM=noreply@alrabei.com

# =====================================================
# DEPLOYMENT INSTRUCTIONS
# =====================================================
# 
# 1. BEFORE DEPLOYING:
#    [ ] Generate strong JWT_SECRET (64 characters)
#    [ ] Set up production PostgreSQL database with SSL
#    [ ] Configure FRONTEND_URL to your domain
#    [ ] Update CORS_ORIGINS with your domains
#    [ ] Enable database backups
#
# 2. SET IN HOSTING PLATFORM (Railway, Render, Vercel, etc):
#    - Copy all variables above to your hosting platform
#    - DO NOT commit actual values to git
#    - Use hosting platform's secret management
#
# 3. VERIFICATION:
#    - Test health endpoint: https://api.yourdomain.com/health
#    - Check logs for errors
#    - Verify database connection
#    - Test authentication flow
#
# 4. MONITORING:
#    - Set up error tracking (Sentry, Rollbar, etc)
#    - Enable logging to external service
#    - Monitor database performance
#    - Set up alerts for critical errors
#
# =====================================================
