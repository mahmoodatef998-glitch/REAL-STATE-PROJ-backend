# =====================================================
# AL RABEI REAL ESTATE - Backend Configuration
# =====================================================
# DEVELOPMENT: Copy this file to 'config.env' and fill in the values
# PRODUCTION: Use environment variables (DO NOT use config files!)
# DO NOT commit config.env to version control!
# =====================================================

# ============ Server Configuration ============
# Port number for the server to listen on
# Development: 3050 | Production: from environment
PORT=3050

# Environment: development, production, test
# CRITICAL: Must be 'production' for production deployments
NODE_ENV=development

# Log Level: error, warn, info, http, debug
# Production recommended: warn or info
LOG_LEVEL=info

# ============ Frontend Configuration ============
# Frontend URL for CORS configuration
# Development: http://localhost:3000
# Production: https://yourdomain.com
FRONTEND_URL=http://localhost:3000

# Additional CORS origins (comma-separated)
# Development: http://localhost:3000,http://localhost:3001
# Production: https://yourdomain.com,https://app.yourdomain.com
# CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ============ Security Configuration ============
# ⚠️ CRITICAL: JWT Secret Key
# Generation: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# Requirements:
#   - Minimum 32 characters (recommended: 64+ characters)
#   - Use strong random value (NOT readable words)
#   - MUST be unique for each environment
#   - NEVER commit to version control
#   - MUST be set in production environment variables
# Development Example (CHANGE THIS):
JWT_SECRET=DEV-ONLY-CHANGE-THIS-IN-PRODUCTION-min32chars1234567890

# Rate Limiting Configuration
# Development: 100 requests per 15 minutes
# Production: 50 requests per 15 minutes (more strict)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ============ Database Configuration ============
# ⚠️ CRITICAL: PostgreSQL Database Connection URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# 
# Development (Local):
#   postgresql://postgres:password@localhost:5432/al_rabei_real_estate
#
# Production (Recommended Services):
#   - Supabase: https://supabase.com (PostgreSQL managed)
#   - Railway: https://railway.app
#   - Neon: https://neon.tech
#   - AWS RDS: https://aws.amazon.com
#
# IMPORTANT: Always use SSL=true in production
# Example: postgresql://user:pass@host:5432/db?sslmode=require
#
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/al_rabei_real_estate

# ============ File Upload Configuration ============
# Maximum file size for uploads (in bytes)
# Default: 10MB = 10485760 bytes
MAX_FILE_SIZE=10485760

# Allowed file types for property images
ALLOWED_FILE_TYPES=image/jpeg,image/jpg,image/png,image/webp

# ============ Email Configuration (Optional) ============
# SMTP settings for email notifications
# Services: Gmail, SendGrid, AWS SES, Mailgun, etc.
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password
# EMAIL_FROM=noreply@alrabei.com

# ============ Production Deployment Checklist ============
# Before deploying to production, ensure:
# 
# [ ] NODE_ENV=production
# [ ] JWT_SECRET set to a strong random value (64+ chars)
# [ ] DATABASE_URL points to production database (with SSL)
# [ ] FRONTEND_URL set to production domain (https://)
# [ ] CORS_ORIGINS updated with production domains
# [ ] RATE_LIMIT_MAX_REQUESTS reduced (50 recommended)
# [ ] Database backups configured
# [ ] SSL/TLS enabled
# [ ] Monitoring and logging set up
# [ ] All environment variables set in hosting platform
# [ ] config.env NOT committed to git
# [ ] .gitignore includes config.env
# [ ] Test health check: http://your-api/health

